/**
 * @providesModule client-action-types
 */

import keyMirror from "keymirror";

export const ClientConstant = {
 ActionTypes: keyMirror({
    GET: null,
    ADD: null,
    DELETE: null,
    UPDATE: null,
  })
};
